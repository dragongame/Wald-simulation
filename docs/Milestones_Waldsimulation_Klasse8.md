# Milestones: Waldökosystem-Störungssimulation (Klasse 8)

**Zweck:** Lebendes Tracking-Dokument für den Umsetzungsfortschritt. Wird von Claude Code während der Umsetzung aktualisiert (Status-Spalte pflegen, Häkchen setzen, neue Erkenntnisse ergänzen). Dupliziert keine Inhalte aus den Quelldokumenten, sondern verlinkt darauf.

**Quellenlage (Rangfolge, siehe Umsetzungsauftrag Abschnitt 0):**
1. `Umsetzungsauftrag_Waldsimulation_Klasse8_fuer_Claude_Code.md` – Entscheidungsprotokoll, Datenmodell, Akzeptanzkriterien
2. `Mitteleuropäische_Waldökosysteme_Wissensbasis_v2.md` – fachliche Quelle (Arten, Zahlen, Kaskaden)
3. `Styleguide_Waldsimulation_Klasse8.md` – Farbe/Typografie/Illustration/Animation (**seit dieser Session vollständig vorhanden**, vorher fehlend)
4. `Illustrations_Promptvorlage_Waldsimulation_Klasse8.md` – Sprite-Dateinamen/-Zustände
5. `Technische_Anforderungen_Waldsimulation_Klasse8.md` + `User_Stories_Waldsimulation_Klasse8.md` – Hintergrund, an mehreren Stellen überschrieben

Ergänzend: `Fehlende_Grafiken_Bekannt_...md` (bekannte fehlende Bilder mit Prompts), `Fehlende_Grafiken_Laufend_...md` (von Claude Code laufend zu pflegende Liste neu entdeckter Bedarfe) und `Ideen_Backlog_Waldsimulation_Klasse8.md` (vom Nutzer gepflegte Sammelstelle für Ideen/Verbesserungswünsche für spätere Versionen, außerhalb des aktuellen Umsetzungsauftrags).

---

## Bekannte Annahmen (nicht mit echter Lehrkraft geprüft)

Diese drei Werte sind in den Quelldokumenten explizit als „Annahme, mit Lehrkraft abzugleichen" markiert. Sie werden für die Umsetzung wie dokumentiert übernommen (Nutzer-Entscheidung vom 2026-08-25), sollten aber vor dem echten Unterrichtseinsatz noch mit einer Lehrkraft gegengeprüft werden.

| Annahme | Wert | Quelle |
|---|---|---|
| Zeitabstand Sturm → Trockenheit | 1 Jahr | Umsetzungsauftrag 2.6 |
| Zeitabstand Sturm → Temperatur (bei Kombination Temperatur+Sturm) | Sturm-Trigger bei Jahr 1 | Umsetzungsauftrag 2.6 / User Stories 3.5 |
| Zeitabstand Totholzentnahme + (Borkenkäfer/Trockenheit/Sturm) | 0 Jahre (gleichzeitig) | Umsetzungsauftrag 2.10.4 |

---

## Milestones

| # | Milestone | Status | Bezug (Akzeptanzkriterien/Abschnitte) |
|---|---|---|---|
| M0 | **Content-Fundament**: Sprites aus Atlas extrahiert (`assets/sprites/`, 38 Dateien, `hirsch`→`rothirsch` vereinheitlicht), Styleguide integriert, Prompt-Dateien für alle 17 bekannten fehlenden Bilder (`assets/prompts/`), Milestone-Doku angelegt | ✅ fertig (2026-08-25) | Vorbereitung für alle folgenden Milestones |
| M1 | **Datenmodell**: `data/nodes.json` (32 Knoten inkl. `mensch_bewirtschaftung` + 3 abiotische Sonderknoten Licht/Wasser/Temperatur), `data/edges.json` (59 Kanten, Beziehungstabelle inkl. Kanten-Transformationsregel für Habicht/Sperber, Luchs/Wolf, Raupen/Blattläuse), `data/waldtypen.json` (3 Waldtypen mit Resilienzfaktoren), `data/stoerungen.json` (5 Ereignis-Störungen, Wildverbiss-Regler, Zeitabstands-/Kombinationsregeln) als single source of truth, referentiell geprüft | ✅ fertig (2026-08-25) | Umsetzungsauftrag 4.1–4.4, Wissensbasis Abschnitt 2–3 |
| M2 | **Build-Time-Simulationsmodul**: geschlossenes Berechnungsmodul (`scripts/simulation/model.py`) + Build-Skript (`scripts/simulation/build_simulationen.py`), erzeugt alle 195 gültigen Waldtyp×Konfiguration-Kombinationen als statische Zeitreihen-JSONs unter `data/generated/simulationen/` (+ Index), Akzeptanzkriterium „Borkenkäfer+Fichtenmonokultur=Kollaps, Borkenkäfer+Mischwald=lokal" und „Wildverbiss erst in 2. Hälfte sichtbar" verifiziert | ✅ fertig (2026-08-25) | Umsetzungsauftrag 2.11, Akzeptanzkriterium „195 Kombinationen vorab berechnet" |
| M3 | **PWA-Grundgerüst**: `manifest.json`, `index.html`/`css/styles.css`/`js/app.js` als App-Shell, Service Worker (`sw.js`, inhaltshash-versioniert über `sw-precache-manifest.json`, `skipWaiting`/`clients.claim`), konsequent relative Pfade (GitHub-Pages-Unterordner), `js/storage.js` als localStorage-Grundlage fürs spätere Forscherheft | ✅ fertig (2026-08-26) | Technikdokument 5.1–5.2, 5.7 |
| M4 | **Start & Auswahl**: zwei Pflicht-Wald-Plätze (zwingend unterschiedliche Typen, gegenseitig deaktiviert), freie Störungsauswahl (0–2 von 5 Ereignis-Typen, Kategorie-Icons/-Rahmen Naturereignis/Bewirtschaftung, Reihenfolge-Auswahl nur wenn `reihenfolge_waehlbar`), Wildverbiss-Regler (3 Stufen), Hypothese je Wald, Play-Sperre bei „keine Abweichung vom Ausgangszustand", löst Auswahl live zur passenden vorab berechneten Zeitreihe auf (Vorschau-Screen mit Jahr-0/Jahr-20-Werten, Live-Dashboard folgt in M5) | ✅ fertig (2026-08-26) | Umsetzungsauftrag 2.1/2.2/2.10, 3.2 |
| M5 | **Live-Dashboard**: Feldmessinstrumente-Optik (6 Indikatoren, Symbol+Skala+Text, nie Farbe allein), Zeitsteuerung (Play/Pause/Einzelschritt + Zeitachsen-Regler, Jahr 0–20), Art-Sprite-Zustandswechsel mit Crossfade (inkl. Sukzession, z. B. Birke erscheint erst im Verlauf), Vergleichsansicht zweier Wälder nebeneinander, kurzer Abschluss-Hinweis am Ende des Durchlaufs | ✅ fertig (2026-08-26) | Umsetzungsauftrag 3.3, Styleguide Abschnitt 6 |
| M6 | **Analyse-Screen**: eigener Vollbild-Screen (getrennt vom Dashboard, erreichbar über Button im Abschluss-Hinweis bei Jahr 20), frei kombinierbare Kurven für alle 14 Indikatoren (nicht nur die 6 Dashboard-Instrumente), kaskadenrelevante Kurven vorausgewählt/mit ★ hervorgehoben, bis zu 3 Kurven-Kombinationen als Snapshot merkbar (Übernahme ins Forscherheft folgt mit M7) | ✅ fertig (2026-08-26) | Umsetzungsauftrag 2.5, User Stories 3.7/3.8 |
| M7 | **Forscherheft**: eigener Reflexions-Screen nach der Analyse (Reflexionsfragen wörtlich aus Wissensbasis 6.5, inkl. Zusatzfrage bei Totholzentnahme), dauerhafte lokale Speicherung als Datenmodell v2 (2× Wald-Datensatz inkl. automatischem Endzustandsbild/Kurzbeschreibung, Störung(en) inkl. Reihenfolge/Zeitpunkt, Regler, bis zu 3 Kurven-Snapshots, gemeinsamer Reflexionstext, Feldbuch-Stempel als Signatur-Element), Übersichts-Screen mit Fortschrittsanzeige ohne festen Zielwert, Textexport je Seite (Kopieren/Teilen), „neue Sitzung" (alle Einträge löschen, mit Bestätigung), von überall über Button auf dem Startbildschirm erreichbar | ✅ fertig (2026-08-26) | Umsetzungsauftrag 2.4/2.9, Technikdokument 5.6 |
| M8 | **Netzwerk-Graph & Rachel Carson**: Graph zu Beginn gesperrt, Button „Wissenschaftler:in um Hilfe bitten", tolerante Namensprüfung, editierbare Brief-Content-Datei, Kaskadenpfad-Hervorhebung als „Tinten-Spur", Vergleichsansicht als Doppelseite | ✅ fertig (2026-08-26) | Umsetzungsauftrag 2.3, Styleguide Abschnitt 5 |
| M9 | **Arten-Lexikon**: alle 32 Knoten, nach Kategorie gruppiert, Bild + 1–2 Fakten, ohne Suche, unabhängig vom Graphen aufrufbar | ✅ fertig (2026-08-26) | Umsetzungsauftrag 2.8 |
| M10 | **Asset-Feinschliff**: restliche extern generierte Bilder einpflegen sobald geliefert (17 Prompt-Dateien aus M0 + laufend ergänzte Liste), Beobachtungs-Stempel, Animationen gemäß Styleguide Abschnitt 7 | ✅ fertig (2026-08-26) | Fehlende_Grafiken_Bekannt/_Laufend, Styleguide Abschnitt 4/6/7 |
| M11 | **Test & Deployment**: Abgleich gegen alle Akzeptanzkriterien (Umsetzungsauftrag Abschnitt 6), GitHub Pages Deployment, QR-Code für die Klasse | ✅ fertig (2026-08-26) | Umsetzungsauftrag Abschnitt 6, Technikdokument 5.7 |
| M12 | **Dashboard-Kacheln für die 20 neuen Arten-Indikatoren**: eigene Live-Dashboard-Kacheln/Sprites für die in der Analyse-Erweiterung (siehe „Erweiterung (2026-08-26), 20 neue Arten-Indikatoren" unten) simulierten Arten, die bisher nur als Kurve wählbar sind | ✅ fertig (2026-08-26) | Backlog „Mehr Tiere und Pflanzen bei der Simulation anzeigen" (Teil-Erledigung) |
| M13 | **Reihenfolge-Abfrage nur bei echtem Zeitversatz**: Reihenfolge-Auswahl in der UI nur noch anbieten, wenn `abstand_jahre > 0` (z. B. Sturm+Borkenkäfer, Sturm+Trockenheit); bei `abstand_jahre = 0` (aktuell nur Trockenheit+Borkenkäfer betroffen) entfällt die Frage, nachdem verifiziert ist, dass beide Reihenfolgen bei echter Gleichzeitigkeit identische Zeitreihen liefern | ✅ fertig (2026-08-26) | Backlog „Szenarios wo die Reihenfolge egal ist" |
| M14 | **Einfachere Wald-Auswahl**: statt fester Plätze „Wald 1"/„Wald 2" alle drei Waldtypen nebeneinander zeigen, zwei davon anwählen, Reihenfolge der Anwahl (links→rechts) bestimmt automatisch Wald 1/Wald 2 | ⬜ offen | Backlog „Einfachere Auswahl" |
| M15 | **Netzwerk-Graph übersichtlicher**: Sidebar mit an-/abwählbaren Art-Icons zum Filtern der im Graphen gezeigten Knoten, deutlicher unterscheidbare Kanten-/Linienarten | ⬜ offen | Backlog „Netzwerk übersichtlicher gestalten" |
| M16 | **Graphen-Snapshots im Forscherheft anzeigen**: die bis zu 3 gespeicherten Kurven-Snapshots (seit M6/M7 als Daten im Forscherheft-Eintrag vorhanden) als tatsächliche Kurvengrafik auf der Forscherheft-Seite rendern, nicht nur referenzieren | ⬜ offen | Backlog „Die Graphen sollen im Forscherheft angezeigt werden" |
| M17 | **Instrumente als Tacho/Radialdiagramm**: Live-Dashboard-Instrumente (M5) optional/zusätzlich als Radialdiagramm/Tacho mit Art/Baum in der Mitte statt nur Symbol+Skala+Text | ⬜ offen | Backlog „Instrument Verbesserung" |
| M18 | **Drag & Drop für Szenario-Zusammenstellung**: Wälder/Störungen per Drag & Drop ins Forscherheft/die Auswahl ziehen statt nur anzuklicken | ⬜ offen | Backlog „Drag & Drop" |
| M19 | **Lehrkraft-Liste interessantester 2er-Kombinationen**: Übersicht/Empfehlung besonders kontrastreicher Wald×Störung-Kombinationen zur gezielten Verteilung an Gruppen | ⬜ offen | Backlog „Erstelle eine Liste den Interessantesten 2er Kombinationen" |
| M20 | **Feldbuch-Feeling**: offene Buchseiten-Optik bei Szenario-Zusammenstellung (siehe M18/M14) und Reflexion (M7), Post-it-Navigation, `docs/eingang/Hintergrund.png` als Journal-Hintergrund, `docs/eingang/Bockmarks-3.png` als Bookmark-Navigation, Prüfung/Anpassung der Regel „Netzwerk erst nach Wissenschaftler:in-Freischaltung sichtbar" (M8) | ⬜ offen | Backlog „Mehr Feldbuch feeling" |
| M21 | **PDF-Export des Forscherhefts**: Layout-/Bild-Export statt des bisherigen einfachen Textexports (M7) | ⬜ offen | Umsetzungsauftrag 2.9 (explizit auf „nicht Version 1" vertagt) |
| M22 | **Vorführ-Modus für die Lehrkraft**: z. B. Beamer-Demo-Ablauf vor der eigentlichen Arbeitsphase | ⬜ offen | Umsetzungsauftrag 2.9 (explizit auf „nicht Version 1" vertagt) |

**Priorisierung (Stand 2026-08-26):** M11, M13 und M12 sind abgeschlossen (siehe „Bekannte Lücken" unten für Details). Nächster Schritt laut Reihenfolge: **M16**. Reihenfolge für M12–M22 festgelegt (Claude-Entscheidung, siehe Begründung im „Bekannte Lücken"-Eintrag „Priorisierungsentscheidung" unten):

1. **M13** – kleinster, unabhängiger Fix, keine offenen Fragen mehr.
2. **M12** – direkte Fortsetzung der zuletzt bearbeiteten Analyse-Erweiterung, Sprites bereits vorhanden (Lexikon/M9).
3. **M16** – schließt die Forscherheft/Analyse-Funktionalität ab (M6/M7), technisch eng an Bestehendes angelehnt.
4. **M19** – eigenständiges, risikoarmes Lehrkraft-Werkzeug, unabhängig vom restlichen UI.
5. **M14** – vereinfacht die Auswahl-Struktur, bevor M20 sie optisch neu einkleidet.
6. **M15** – eigenständige Graph-Verbesserung, kein Bezug zu den übrigen Punkten.
7. **M20** – große Baustelle (Feldbuch-Feeling); bewusst nach den kleineren Punkten, damit sie nicht zweimal angefasst werden.
8. **M18** – Drag & Drop zielt laut Backlog-Idee explizit auf „das Buch" – ergibt erst nach M20 Sinn.
9. **M17** – rein dekorative Dashboard-Verbesserung, keine Abhängigkeiten, aber auch kein dringender Bedarf.
10. **M21** – ursprünglich für „nicht Version 1" vertagt, größerer Aufwand (PDF-Bibliothek lokal einbinden).
11. **M22** – ursprünglich für „nicht Version 1" vertagt, betrifft nur den optionalen Lehrkraft-Vorführmodus.

Status-Legende: ⬜ offen · 🔶 in Arbeit · ✅ fertig

---

## Bekannte Lücken / offene Nachlieferungen

- ~~16 Bilder aus `Fehlende_Grafiken_Bekannt` sind weiterhin nicht vorhanden~~ **Erledigt (2026-08-26):** Alle 17 zuvor fehlenden Bilder (9× Liste A Wald-Zustandsbilder, 4× Liste B Störungs-Icons, 1× Liste D Mensch-Icon, 1× Liste E Wildverbiss-Waldbild, 1× App-Icon, plus das während M5 nachgetragene `stoerung_wildverbiss.png`) wurden von der Lehrkraft extern generiert, über `docs/eingang/` übergeben und unter den dokumentierten Dateinamen in `assets/sprites/` abgelegt; das App-Icon wurde aus dem gelieferten Master-Bild (jetzt unter `assets/sprites/_reference/app_icon_master.png`, nicht Teil des Precache) in die drei benötigten Auflösungen skaliert (`assets/icons/icon-192.png`, `icon-512.png`, `apple-touch-icon.png`). Referenzielle Prüfung (jede in `data/*.json` referenzierte Sprite-Datei existiert) bestanden. Alle `<img>`-Fallbacks (Emoji+Label) greifen dadurch jetzt nur noch im Ausnahmefall. **Neuer Hinweis:** Die 16 neuen Dateien sind mit zusammen ca. 51 MB deutlich größer als der bisherige Sprite-Satz (unkomprimierte Aquarell-Illustrationen); sollte sich das erste Service-Worker-Install auf langsamem WLAN als spürbar langsam erweisen, sind sie ein guter Kandidat für eine verlustarme Verkleinerung/WebP-Konvertierung (M10).
- Das Referenzbild `docs/ChatGPT Image ... 06_18_55 PM.png` (Wald-Szene mit Fichte) wurde **nicht** als `wald_mischwald_stabil.png` übernommen (enthält eine Fichte, widerspricht dem didaktisch zentralen „fichtenfreier Referenz-Mischwald"); liegt nur als Stilreferenz unter `assets/sprites/_reference/wald_szene_referenz.png`.
- **M1-Datenmodell, offene Modellierungsentscheidung für M8:** Für die beiden in Szenario 5/6 neuen Kantentypen (`bewirtschaftung`: Mensch entnimmt Totholz; `strukturell`: Verbiss verhindert/entwertet Verjüngung) definiert der Styleguide (vor Szenario 5/6 verfasst, nur 5 Grundtypen) noch keine Farbe/kein Symbol – siehe `data/edges.json` → `_meta.todo_m8`. Muss beim Netzwerk-Graph (M8) ergänzt werden.
- **M2-Tooling-Hinweis:** In dieser Entwicklungsumgebung war kein Node.js verfügbar. Das Berechnungsmodul + Build-Skript für die Vorabsimulation sind deshalb in Python geschrieben (`scripts/simulation/`) statt in der von Technikdokument Abschnitt 6 empfohlenen Sprache. Das läuft ausschließlich beim Build (nie im Browser) und ändert nichts an Datenmodell oder Ausgabeformat – reines, sprachunabhängiges JSON unter `data/generated/simulationen/`. Diese generierten Dateien werden mit committet (kein CI-Build-Schritt vorgesehen, GitHub Pages liefert nur statische Dateien aus).
- ~~M3-App-Icon ist ein Platzhalter~~ **Erledigt (2026-08-26):** Durch das gelieferte, auf drei Auflösungen skalierte echte App-Icon ersetzt (siehe Eintrag oben). `scripts/pwa/generate_placeholder_icon.py` bleibt im Repo als Notfall-Werkzeug, falls je wieder ein Platzhalter gebraucht wird, wird aber nicht mehr aktiv genutzt.
- **M3-Tooling-Hinweis:** Anders als bei M2 dokumentiert war in dieser Session Node.js (v24, via nvm) doch verfügbar; die JS-Dateien der PWA (`sw.js`, `js/*.js`) wurden entsprechend mit `node --check` auf Syntaxfehler geprüft. Ein Browser mit Service-Worker-Unterstützung stand jedoch nicht zur Verfügung – Installation/Offline-Verhalten auf einem echten iPad/Safari vor dem Unterrichtseinsatz noch manuell verifizieren.
- **M3-Build-Schritt, bei jeder Content-/Asset-Änderung nötig:** `sw-precache-manifest.json` (Dateiliste + Inhalts-Hash für den Service-Worker-Cache-Namen) wird von `scripts/pwa/build_precache_manifest.py` erzeugt und mitcommittet. Nach jeder Änderung an `index.html`, `css/`, `js/`, `assets/icons/`, `assets/sprites/` oder `data/` muss das Skript erneut laufen, sonst bleibt der Service Worker auf einem veralteten Cache-Stand.
- ~~M4-Auswahlbildschirm nutzt großteils noch fehlende Bilder~~ **Erledigt (2026-08-26):** Alle Wald-Kartenbilder und Störungs-Icons sind jetzt vorhanden (siehe Eintrag oben) und erscheinen automatisch, ohne Code-Änderung – der `<img>`-Fallback (Klasse `sprite-frame`, Emoji+Textlabel bei Ladefehler, siehe `js/startScreen.js`) bleibt als Absicherung für zukünftige, noch nicht gelieferte Bilder bestehen.
- **M4-Umsetzungsentscheidung, Vergleichsmodus:** Die gewählte(n) Störung(en) + der Wildverbiss-Regler gelten für **beide** Wald-Plätze gleichzeitig (ein gemeinsamer Auswahlschritt, nicht zwei getrennte) – nur der Waldtyp selbst und die Hypothese sind pro Platz individuell. Das ist im Umsetzungsauftrag nicht wortwörtlich festgelegt, ergibt sich aber zwingend aus dem Vergleichszweck (Kontrast Mono- vs. Mischwald unter identischer Störung, Akzeptanzkriterium Abschnitt 6).
- **M4-Umsetzungsentscheidung, Reihenfolge-Kanonisierung:** Bei den drei Ereignispaaren mit `reihenfolge_waehlbar: false` (alle Totholzentnahme-Kombinationen) fragt die UI die Reihenfolge nicht ab, sondern wählt intern eine feste Reihenfolge (nach `EVENT_ORDER` in `js/simulationConfig.js`, deckungsgleich mit `EVENTS_NATUR_UND_BEWIRTSCHAFTUNG` in `build_simulationen.py`) – geprüft, dass beide Reihenfolge-Richtungen identische Zeitreihen liefern (M2-Build erzeugt beide Dateien redundant).
- **M4-Test:** `js/simulationConfig.js`-Auflösung wurde gegen alle 195 generierten Dateien in `data/generated/simulationen_index.json` programmatisch geprüft (alle 285 gültigen Kombinationen aus 3 Waldtypen × 22 Ereigniszuständen × 3 Reglerstufen lösen auf die richtige Datei auf); zusätzlich Ende-zu-Ende mit jsdom durch die echte `index.html`/JS-Pipeline getestet (unter anderem das Akzeptanzkriterium „Borkenkäfer + Fichtenmonokultur" → Baumbestand 95→0, im Mischwald nahezu unverändert; Reihenfolge-Pflicht bei Sturm+Borkenkäfer; Temperatur-Kombination ohne Reihenfolge-Abfrage; isolierter Wildverbiss-Lauf ohne Ereignis; Play-Sperre bei leerer Auswahl).
- **M5-Umsetzungsentscheidung, Reflexionsfrage/Analyse-Screen bewusst NICHT hier:** Umsetzungsauftrag 2.5 beschreibt den Ablauf „Live-Simulation → Analyse-Screen (M6) → Reflexionsfrage"; die Reflexionsfrage selbst ist laut M7-Milestone-Beschreibung außerdem Teil des Forscherheft-Datenmodells v2 (Umsetzungsauftrag 2.4: „ein gemeinsamer Reflexionstext"). Das Dashboard (M5) endet deshalb bewusst nur mit einem kurzen Abschluss-Hinweis (Endwerte vs. Hypothese), nicht mit der eigentlichen Reflexionsfrage oder den frei kombinierbaren Kurven – die kommen mit M6/M7.
- **M5-Umsetzungsentscheidung, Zustands-Schwellenwerte pro Art:** Die Zuordnung von Indikatorwert (0–100) zu Sprite-Zustand (z. B. Fichte: ≥70 gesund, ≥40 gestresst, ≥15 befallen, sonst abgestorben; Buche/Kiefer nur 2 Stufen, Eiche/Birke 1 Stufe gemäß Asset-Abdeckung aus 2.9) ist eine eigene UI-Schwellenwert-Entscheidung, nicht aus einem Quelldokument übernommen – plausibel gewählt, aber nicht mit einer Lehrkraft abgeglichen.
- **M5-Umsetzungsentscheidung, welche Arten pro Wald angezeigt werden:** Eine Art-Kachel erscheint, sobald ihr Indikator irgendwann im 20-Jahres-Verlauf > 0 ist (nicht nur bei Jahr 0) – nötig, damit z. B. die Birken-Sukzession nach Sturm/Borkenkäfer in der Fichtenmonokultur sichtbar wird, obwohl `birke_anteil` bei Jahr 0 dort 0 ist.
- **M5-Test:** Per jsdom Ende-zu-Ende geprüft: Dashboard-Übergang nach „Play", korrekte Art-Kacheln pro Waldtyp (inkl. später erscheinender Birke bei Fichtenmonokultur), Crossfade-Zustandswechsel Fichte gesund→abgestorben über 20 Einzelschritte, `instrument--kollaps`-Klasse bei Baumbestand-Kollaps, funktionierender Play/Pause-Timer über echte Zeit, Zurück-Button. (Ein erster Testlauf ohne Wartezeiten zwischen den Einzelschritten zeigte scheinbar keinen Sprite-Wechsel – das lag an `requestAnimationFrame`, das in der reinen Testschleife nie zum Zug kam, nicht an der App selbst; mit realistischem zeitlichen Abstand zwischen Interaktionen funktioniert der Crossfade korrekt.)
- **M6-Umsetzungsentscheidung, „kaskadenrelevante Kurven":** User Stories 3.7 verlangt, dass die zum tatsächlich abgelaufenen Kaskadenpfad gehörenden Kurven hervorgehoben/vorausgewählt sind. Eine automatische Ableitung aus den `relevante_knoten`-Listen in `data/stoerungen.json` war nicht 1:1 möglich, da mehrere dort genannte Knoten (z. B. Buntspecht, Hallimasch, Ameisenbuntkäfer, Zunderschwamm, Eichelhäher) keinen eigenen numerischen Indikator in `data/indikatoren.json` haben. Stattdessen wurde je Störung/Regler eine kuratierte Zuordnung auf die nächstliegenden Indikatoren vorgenommen (`KASKADE_JE_STOERUNG`/`KASKADE_WILDVERBISS` in `js/analyse.js`) – plausibel aus der Kaskadenbeschreibung der Wissensbasis abgeleitet, aber eine eigene UI-Entscheidung, nicht 1:1 aus einem Quelldokument übernommen.
- **M6-Umsetzungsentscheidung, Snapshot-Speicherung (seit M7 überholt):** In M6 lebten Snapshots noch bewusst nur im Arbeitsspeicher; seit M7 werden sie über „Weiter zur Reflexion" → „Im Forscherheft speichern" dauerhaft mit der Forscherheft-Seite persistiert (localStorage, siehe M7-Einträge unten).
- **M6-Test:** Per jsdom Ende-zu-Ende geprüft: Übergang Dashboard (Jahr 20) → Analyse-Screen, 14 Indikator-Checkboxen mit korrekter Vorauswahl für das Szenario, Kurven-Anzahl im SVG-Chart ändert sich synchron mit Checkbox-Toggle, Snapshot-Limit (3) inkl. Deaktivierung und Hinweistext, Snapshot-Entfernen, Rücksprung zum Dashboard.
- **Kritischer Fix nach Nutzer-Feedback (2026-08-26), Service-Worker-Update-Erkennung:** Der Button „Zur Kurven-Analyse" (M6) tat auf dem echten iPad nichts. Ursache: `sw.js` selbst hatte sich seit M3 nie geändert (nur die von ihm geladene `sw-precache-manifest.json`) - Browser erkennen ein Service-Worker-Update aber ausschließlich per Byte-Vergleich der Service-Worker-Datei selbst, nicht anhand der von ihr nachgeladenen Inhalte. Dadurch installierte kein Browser je eine neue Service-Worker-Version, und alle Nutzer blieben für immer auf dem allerersten gecachten Stand hängen, unabhängig davon, wie oft neue Inhalte deployt wurden. **Fix:** `sw.js` wird jetzt selbst aus einem Template (`scripts/pwa/sw.template.js`) mit eingesetzter Versionsnummer generiert (nie mehr von Hand editieren), sodass sich seine Bytes bei jeder Content-Änderung garantiert mitändern. Zusätzlich dabei entdeckt: `SHELL_FILES` im Build-Skript war von Hand gepflegt und seit M4 nicht mehr aktualisiert worden - `js/data.js`, `ui.js`, `icons.js`, `simulationConfig.js`, `startScreen.js`, `dashboard.js`, `analyse.js` fehlten dadurch komplett im Offline-Cache (Verstoß gegen Technikdokument 5.2). Auch das behoben: `js/` und `css/` sind jetzt wie `assets/`/`data/` verzeichnisbasiert erfasst statt einzeln aufgelistet, damit neue Dateien nicht mehr vergessen werden können. Für bereits betroffene Geräte genügt ein normales Neuladen der Seite (skipWaiting/clients.claim übernehmen die neue Version automatisch), kein manuelles Cache-Löschen nötig.
- **M7-Umsetzungsentscheidung, Endzustandsbild-Zuordnung:** User Stories 3.7 verlangt „automatisch je Wald ein Endzustandsbild mit Kurzbeschreibung". Die Zuordnung Indikatorwert → Bilddatei (`wald_<typ>_stabil/geschaedigt/kollabiert.png` anhand `gesamtvitalitaet`-Schwellen 34/67, wie bereits im Dashboard verwendet; Sonderfall `wald_mischwald_verarmt_reinbestand.png` bei Mischwald + Wildverbiss-Regler≠niedrig + spürbar gesunkener `verjuengung_mischbaumarten` bei weiterhin hoher `gesamtvitalitaet`) ist eine eigene, plausible UI-Entscheidung (`js/forscherheft.js`, `endzustandBild()`), nicht aus einem Quelldokument übernommen. Alle referenzierten Bilddateien sind seit 2026-08-26 vorhanden (siehe „Bekannte Lücken" oben); der Sprite-Fallback (Emoji+Label) bleibt als Absicherung bestehen, greift hier aber im Regelfall nicht mehr.
- **M7-Umsetzungsentscheidung, Reflexion nicht Pflichtfeld:** Wie schon die Hypothese (M4) ist auch der Reflexionstext nicht zwingend zum Speichern erforderlich – kein Quelldokument fordert das explizit, und ein leeres Feld wird im Forscherheft transparent als „(keine Reflexion angegeben)" angezeigt statt das Speichern zu blockieren.
- **M7-Umsetzungsentscheidung, Reichweite von „neue Sitzung":** Löscht ausschließlich die gespeicherten Forscherheft-Einträge (mit Bestätigungsdialog), nicht die aktuell offene Auswahl auf dem Startbildschirm - passend zur Formulierung in Technikdokument 5.6 ("Möglichkeit, eine neue Sitzung zu starten"), die sich im Kontext auf die gespeicherten Forscherheft-Daten bezieht.
- **M7-Test:** Per jsdom Ende-zu-Ende geprüft: Forscherheft schon vor dem ersten Durchlauf leer aufrufbar, kompletter Fluss Auswahl→Dashboard→Analyse (inkl. Snapshot)→Reflexion→Speichern, korrekte Endzustandsbild-Zuordnung (u. a. `wald_fichtenmonokultur_kollabiert.png` beim Borkenkäfer-Kollaps-Szenario), Zähler/Fortschrittsanzeige, Textexport-Inhalt, Kopieren-Button, mehrere Durchläufe hintereinander bei erhaltenem Auswahl-Zustand, „Neue Sitzung" sowohl abgebrochen (keine Löschung) als auch bestätigt (alles gelöscht) getestet.
- **M9-Umsetzungsentscheidung, Kategorie-Reihenfolge/-Bezeichnung:** Die 9 Gruppen (Bäume, Sträucher, Pilze, Krautschicht, Pflanzenfresser, Samenverbreiter, Prädatoren, Zersetzer, Mensch) folgen exakt der `kategorie`-Reihenfolge, in der die Knoten bereits in `data/nodes.json` stehen (keine eigene Sortierung) - deutscher Plural-Titel je `kategorie`-Wert ist eine eigene, plausible UI-Beschriftung, nicht aus einem Quelldokument übernommen. Die drei abiotischen Sonderknoten (Licht/Wasser/Temperatur) werden über das Feld `zaehlt_zu_32_knoten` ausgefiltert (Umsetzungsauftrag 2.7/2.8: zählen nicht zu den 32 Lexikon-Knoten).
- **M9-Umsetzungsentscheidung, kein Tap-Steckbrief nötig:** Anders als im Netzwerk-Graph (M8), wo Antippen eines Knotens einen Steckbrief einblendet, zeigt das Lexikon Bild + Fakten direkt auf der Karte, ohne weitere Interaktion - passend zu "einfache Liste" (Umsetzungsauftrag 2.8) und weil hier (anders als im Graphen) kein Platzmangel durch ein Netzwerk-Layout besteht.
- **M9-Test:** Per jsdom geprüft: Lexikon ohne jede Freischaltung direkt vom Startbildschirm erreichbar, exakt 32 Karten in 9 Kategorie-Gruppen, kein abiotischer Sonderknoten enthalten, jede Karte hat mindestens einen Fakt und ein Bild, kein Such-Eingabefeld vorhanden, Zurück-Button funktioniert.
- **M1-Datenmodell, dokumentierte Vereinfachungen:** Wo die Wissensbasis nur generische Ziele nennt („Kraut", „Vögel", „Kleinsäuger", „Bäume", „geschwächter Baum") statt konkreter Arten, wurden nachvollziehbare, im jeweiligen `hinweis`-Feld begründete Zuordnungen getroffen (z. B. Hallimasch-Befall generisch auf Fichte angewendet). Keine Erfindung neuer Fachinhalte, nur Konkretisierung bestehender Tabellenzeilen – im Zweifel in `data/edges.json` nachlesen.
- **M8-Umsetzungsentscheidung, Farbe/Symbol für „bewirtschaftung"/„strukturell":** Löst den in `data/edges.json` (`_meta`) dokumentierten M8-TODO auf. Neue CSS-Variablen `--bewirtschaftung` (#B8901F, Sägespäne-Gold) und `--strukturell` (#A85C7C, gedämpftes Verbiss-Malve) in `css/styles.css`, bewusst deutlich unterscheidbar von den fünf Styleguide-Farben; neue Symbol-Pfade `saege`/`huf` in `js/icons.js`. Eigene, plausible Ergänzung, da der Styleguide (Abschnitt 5) vor Szenario 5/6 verfasst wurde und nur die ursprünglichen fünf Beziehungstypen kennt.
- **M8-Umsetzungsentscheidung, Kaskadenpfad-Ermittlung:** Der als „Tinten-Spur" hervorgehobene Pfad wird nicht pro Störung von Hand kuratiert (das wäre bei 59 Kanten sehr fehleranfällig), sondern per Breitensuche entlang der für die gespielte(n) Störung(en)/den Regler relevanten Kanten (`data/stoerungen.json` → `relevante_knoten`, bereits in M6 fürs Analyse-Screen genutzt) ab einem je Störungstyp kuratierten Startknoten (`STARTKNOTEN_JE_STOERUNG`/`STARTKNOTEN_WILDVERBISS` in `js/graph.js`, z. B. Trockenheit → „wasser", Borkenkäfer → „borkenkaefer"). Bei zwei kombinierten Störungen läuft die Suche nacheinander in der zeitlichen Reihenfolge der `trigger_jahr`-Werte los. Dadurch bleiben nicht alle Knoten der relevanten Teilmenge zwangsläufig Teil der Kaskade (z. B. Birke/Eichelhäher bei „Sturm" sind laut Wissensbasis-Kaskadenbeschreibung relevant, aber im reduzierten Beziehungsnetzwerk aus `data/edges.json` nicht direkt mit Fichte/Borkenkäfer verbunden) – das ist eine ehrliche Konsequenz aus dem tatsächlich modellierten Beziehungsnetz, keine Modellierungslücke von M8. Per jsdom-Test von Hand nachgerechnet (Sturm+Borkenkäfer im Fichtenmonokultur-Kontext: 8 relevante Knoten, 6 davon Teil der Kaskade) und bestätigt.
- **M8-Umsetzungsentscheidung, Pseudo-Gruppenknoten:** Vier Kanten in `data/edges.json` haben statt eines konkreten `ziel` nur eine `ziel_kategorie` (Kraut/Kleinsäuger/Vögel allgemein/Baumverjüngung allgemein – siehe dortiges `_meta.generisches_ziel_hinweis`). Damit der Graph trotzdem alle 59 Kanten zeigen kann, bekommen diese vier Gruppen eigene, gestrichelt gerahmte „Sammelgruppen"-Knoten (`GRUPPEN_KNOTEN` in `js/graph.js`) ohne Sprite und ohne Steckbrief-Fakten, nur mit kurzer Erklärung. Zählen nicht zu den 32 Lexikon-Knoten (M9).
- **M8-Umsetzungsentscheidung, Layout:** Reines UI-Layout ohne fachliche Aussage: Knoten werden nach `kategorie` in Zeilen gruppiert (grobe Trophie-Reihenfolge oben→unten: Abiotisch, Bäume, Sträucher, Kraut, Pilze, Totholz & Mensch, Pflanzenfresser & Samenverbreiter, Prädatoren, Sammelgruppen), da ein automatisches Kräfte-/Force-Layout auf einem iPad ohne externe Bibliothek (Technikdokument: keine unnötigen Abhängigkeiten, Performance hat Vorrang) nicht robust umsetzbar war. Bei „vollständiges Netzwerk" (bis zu 7 Karten in einer Zeile) scrollt der Canvas-Container horizontal (`overflow-x: auto`), bei der meist deutlich kleineren relevanten Teilmenge passt er in der Regel ohne Scrollen.
- **M8-Umsetzungsentscheidung, Vergleichsansicht zeigt dieselbe Kaskade auf beiden Feldbuch-Seiten:** Das Beziehungsnetzwerk ist unabhängig vom Waldtyp (dieselben ökologischen Mechanismen wirken in Fichtenmonokultur wie Mischwald – nur die simulierte Stärke der Auswirkung unterscheidet sich, die die Nutzer:innen bereits im Dashboard/in der Kurven-Analyse gesehen haben). Die Doppelseite (Styleguide Abschnitt 5) zeigt deshalb bewusst denselben Kaskadenpfad zweimal nebeneinander, mit dem jeweiligen Waldtyp als Kopfzeile – das unterstreicht „gleicher Mechanismus, unterschiedliche Resilienz" statt eine im Datenmodell nicht vorhandene graphtopologische Differenzierung vorzutäuschen.
- **M8-Umsetzungsentscheidung, „durchgängig sichtbarer" Button:** Wie schon der Forscherheft-Button (M7) nur im Start-Bildschirm-Header platziert (`netzwerk-oeffnen-button`), nicht auf jedem einzelnen Screen einzeln dupliziert – der Startbildschirm ist der zentrale Rückkehrpunkt der App. Zusätzlich pro Forscherheft-Seite ein eigener „Im Netzwerk ansehen"-Button (öffnet bei noch fehlender Freischaltung ebenfalls das Namens-Eingabefeld).
- **M8-Umsetzungsentscheidung, Brief erneut lesbar:** Der Brief wird laut User Stories 3.3 „einmalig" beim Freischalten gezeigt; zusätzlich gibt es auf dem Graph-Screen einen „Brief von Rachel Carson erneut lesen"-Button, da der Inhalt (Lesehilfe für den Graphen) auch später noch nützlich ist. Erneutes Lesen setzt die Freischaltung nicht zurück und wird nicht separat geloggt.
- **Kritischer Fix nach Nutzer-Feedback (2026-08-26), M2-Simulationsmodell: Trockenheit/Temperatur erzeugten kein Totholz:** Beim Live-Testen fiel auf, dass `totholzmenge` in einem Trockenheit+Sturm-Lauf nach dem einmaligen Sturm-Sprung komplett flach blieb, obwohl die Fichte durch die andauernde Trockenheit noch weitere Jahre lang an Vitalität verlor. Ursache in `scripts/simulation/model.py`: Nur Sturm (einmaliger Sprung) und Borkenkäfer (laufend, proportional zum Schaden) waren mit `totholzmenge` gekoppelt; der Vitalitätsverlust durch Trockenheit (`fichte_vitalitaet`/`buche_vitalitaet`) und den dauerhaften Temperatur-Hintergrundeffekt floss dagegen spurlos ab, ohne jede Totholz-Wirkung – ökologisch inkonsistent (absterbende Bäume verschwinden nicht einfach, sie werden zu Totholz). **Fix:** Trockenheit und Temperatur erzeugen jetzt, analog zum Borkenkäfer, Totholz proportional zum tatsächlichen (nach `_clamp` gemessenen) Vitalitätsverlust dieses Schritts (Faktor 0.5, derselbe Faktor wie beim Borkenkäfer). Der Wildverbiss-Effekt bleibt bewusst **nicht** gekoppelt: Er verhindert Verjüngung (junge Bäume wachsen nicht nach), tötet aber keine etablierten Bäume – das ist kein Totholz-Ereignis. Alle 195 Zeitreihen wurden mit `scripts/simulation/build_simulationen.py` neu gebaut; 69 der 195 Dateien haben sich geändert (alle mit aktiver Trockenheit oder Temperatur), die übrigen 126 sind byte-identisch. Erneut geprüft: alle Werte bleiben im Bereich 0–100, das Akzeptanzkriterium „Borkenkäfer + Fichtenmonokultur = großflächiger Kollaps (Fichte → 0), Borkenkäfer + Mischwald = nur lokaler Schaden (Buche/Eiche bei diesem reinen Borkenkäfer-Lauf unverändert bei 65/25, da diese beiden Dateien vom Fix gar nicht betroffen sind)" ist weiterhin erfüllt.
- **Kritischer Fix nach Nutzer-Feedback (2026-08-26), Service-Worker fetch-Handler ("navigate"-Modus):** Erster echter Live-Test auf `dragongame.github.io` nach dem M8-Deploy zeigte `Uncaught ReferenceError: WaldsimAnalyse is not defined` sowie fehlgeschlagene Bild-Requests. Ursache: `scripts/pwa/sw.template.js` reichte im "fetch"-Handler bei einem Cache-Miss `event.request` direkt an `fetch()` weiter. Bei einer Top-Level-Navigation (Seite laden/neu laden) hat `event.request` den Modus `"navigate"` - `fetch()` wirft dafür sofort einen `TypeError` ("'navigate' mode is not allowed", ein bekanntes Service-Worker-Standardproblem), was zu einer instabilen Navigation und dadurch zu Folgefehlern bei parallel nachgeladenen Skripten (u. a. `analyse.js`, daher der `WaldsimAnalyse`-Fehler) und Bildern führen konnte. **Fix:** `fetch(event.request.url)` statt `fetch(event.request)` - eine reine URL-Anfrage hat keinen `"navigate"`-Modus und ist davon nicht betroffen. `sw.js` neu aus dem Template gebaut (`scripts/pwa/build_precache_manifest.py`); die eingebettete Versionsnummer bleibt zufällig identisch (sie hängt nur vom Inhalt der gecachten Assets ab, nicht vom Service-Worker-Code selbst), die Bytes von `sw.js` haben sich aber geändert, was für die Update-Erkennung laut oben beschriebenem Mechanismus ausreicht. Für bereits betroffene Geräte genügt ein normales Neuladen der Seite.
- **M8-Test:** Per jsdom Ende-zu-Ende geprüft (echter statischer HTTP-Server statt `file://`, da `fetch()` sonst nicht funktioniert): falscher Name bleibt gesperrt, tolerante Prüfung bei Groß-/Kleinschreibung und zusätzlichen Leerzeichen schaltet frei, Freischaltung übersteht Bildschirmwechsel (localStorage), Brief-Anzeige, vollständiges Netzwerk zeigt exakt 39 Knoten (35 reale + 4 Sammelgruppen) und alle 59 Kanten, Legende mit 7 Einträgen, Steckbrief bei Knoten- und bei Kanten-Tap, Trigger-Button-Text wechselt nach Freischaltung, echter Forscherheft-„Im Netzwerk ansehen"-Button öffnet die Doppelseite mit korrekt gefilterter Teilmenge (Sturm+Borkenkäfer: 8 relevante Knoten, 6 Kaskade-Knoten – von Hand nachgerechnet und bestätigt), Umschalten auf „vollständiges Netzwerk" funktioniert, Zurück-Navigation führt korrekt zum Forscherheft zurück (nicht zum Startbildschirm), erneutes Lesen des Briefs aus dem Graph-Screen heraus erhält den Vergleichskontext.
- **M10-Umsetzungsentscheidung, dritter Animationsmoment nachgeliefert:** Styleguide Abschnitt 7 verlangt genau drei Animationsmomente: (1) Art-Sprite-Crossfade (M5), (2) Kaskadenpfad-„Tinten-Spur" im Graphen (M8), (3) „Der Stempel-Effekt beim Abschließen eines Durchlaufs". Der Beobachtungs-Stempel selbst (Datum/Waldtyp/Störung) existierte bereits seit M7 (`.feldbuch-stempel` in `css/styles.css`/`js/forscherheft.js`), aber rein statisch, ohne Animation - Moment (3) fehlte. Ergänzt: `.feldbuch-stempel--frisch` (CSS-Keyframe `stempel-aufdruecken`, 0.6s, Skalierung von groß/transparent über leichtes Überschwingen auf die finale Größe, angelehnt an Dauer/Easing der beiden bestehenden Animationen). Die Klasse wird nur beim tatsächlichen Speichern eines neuen Forscherheft-Eintrags gesetzt (`frischGestempeltId` in `js/forscherheft.js`, direkt nach dem Rendern wieder zurückgesetzt) - ein reiner Wechsel zur Übersicht (z. B. über den Header-Button) spielt die Animation nicht erneut ab, nur das tatsächliche „Abschließen eines Durchlaufs" wie im Styleguide gefordert.
- **M10-Test:** Per jsdom Ende-zu-Ende geprüft (echter statischer HTTP-Server): nach dem ersten Speichern trägt genau der neue Stempel die Klasse `feldbuch-stempel--frisch`; nach einem zweiten Speichern hat wieder nur der jeweils neueste Eintrag die Klasse (der erste hat sie verloren); ein reiner Aufruf von `zeigeUebersicht()` ohne neues Speichern setzt die Klasse bei keinem Eintrag.
- **M10-Status Bilder/Stempel-Basiselement:** Beide anderen Teilaufgaben von M10 waren bereits vor dieser Session abgeschlossen (alle Bilder integriert, siehe „Bekannte Lücken" oben unter M0/M9; Stempel-Basiselement seit M7) - diese Session hat nur die fehlende Animation ergänzt und M10 damit vollständig geschlossen.
- **Kritischer Fix nach Nutzer-Feedback (2026-08-26), M2-Simulationsmodell: Bestandsanteil und Vitalität waren dieselbe Zahl:** Beim Live-Testen fiel auf, dass die Fichte im Mischwald von Jahr 0 an immer als „abgestorben" (kaputtes Sprite) dargestellt wurde, obwohl gar keine Störung aktiv war - und dass eine erfolgreich nachwachsende Birke kaum zum angezeigten „Baumbestand (gesamt)" beitrug. Ursache in `scripts/simulation/model.py`: `fichte_vitalitaet`/`buche_vitalitaet`/`eiche_vitalitaet`/`kiefer_vitalitaet` wurden direkt mit dem Bestandsanteil aus `anfangsbestand_anteil_prozent` initialisiert (z. B. Fichte im Mischwald = 5, weil sie dort nur zu 5% beigemischt ist - siehe M1-Begründung für diesen bewusst niedrigen Wert). Die UI (Sprite-Schwellenwerte in `js/dashboard.js`, ≥15 = „befallen", darunter „abgestorben") interpretiert denselben Indikator aber als 0-100-Gesundheitswert - genau wie `data/indikatoren.json` (`_meta.skala`) es für alle Indikatoren vorschreibt: „100 = Maximum/Ausgangszustand". Eine gesunde, nur selten vorkommende Fichte hatte dadurch von Anfang an einen Wert unterhalb der Absterben-Schwelle. Dieselbe Vermischung ließ auch die Birken-Sukzession kaum in `gesamtvitalitaet` einfließen: Das Gewicht jeder Art in der gewichteten Durchschnittsbildung (`_gesamtvitalitaet()`) war der eingefrorene Start-Bestandsanteil, für Birke in Mischwald/Fichtenmonokultur also dauerhaft 0, weshalb nur ein kleiner Korrektur-Faktor („birken_bonus", ×0.02) übrig blieb. **Fix:** Bestandsanteil (Gewicht, wie stark eine Art vertreten ist, bei den vier Nicht-Birke-Arten weiterhin fix, siehe `self._bestandsanteil`) und Vitalität (0-100-Gesundheitswert, startet bei 100 wenn die Art überhaupt vorkommt, sonst 0) sind jetzt getrennte Größen. `_gesamtvitalitaet()` gewichtet Birke mit ihrem tatsächlichen, wachsenden `birke_anteil` statt dem eingefrorenen Startwert; der „birken_bonus"-Hack entfällt. Alle 195 Zeitreihen wurden neu gebaut (alle 195 Dateien geändert, da praktisch jede Kombination mindestens eine Vitalitäts- oder die Birken-Gewichtung berührt). Erneut geprüft: alle Werte bleiben im Bereich 0-100; Akzeptanzkriterium „Borkenkäfer + Fichtenmonokultur = großflächiger Kollaps" weiterhin erfüllt (Fichte 100→0 bis Jahr 10, danach erholt sich der angezeigte Gesamt-Baumbestand durch nachwachsende Birke leicht auf ~14 statt bei 0 zu verharren - deckt sich mit der Wissensbasis-Kaskadenbeschreibung „mittelfristig... Sukzession beginnt (Birke, Brombeere)", ist also eine Verbesserung der fachlichen Treue, keine Abweichung); „Borkenkäfer + Mischwald = nur lokaler Schaden" weiterhin erfüllt (Fichte 100→85.7, `gesamtvitalitaet` 100→99.2 bei Jahr 20 - jetzt zusätzlich sichtbar am Fichte-Sprite selbst, vorher maskierte der sofortige „abgestorben"-Zustand jede echte Schadensdynamik); „Wildverbiss erst in 2. Hälfte deutlich sichtbar" weiterhin erfüllt (Steigungswechsel bei `verjuengung_mischbaumarten` exakt bei Jahr 10 unverändert, da unabhängig von dieser Änderung). `sw-precache-manifest.json`/`sw.js` nach der `data/`-Änderung neu gebaut.
- **Nutzer-Rückmeldung (2026-08-26), kein Bug - bereits im Backlog:** Die Beobachtung „Tiere/Sträucher/Kräuter/Pilze tauchen nur im Netzwerk und Lexikon auf, sind aber im laufenden Wald-Dashboard bzw. in der Kurven-Analyse nicht als eigene Kachel/Kurve wählbar" ist kein Fehler, sondern der aktuelle Modellumfang: `scripts/simulation/model.py` berechnet nur 14 Indikatoren (5 Baumarten + Borkenkäfer-Dichte + Wilddichte + ein paar Ökosystem-Werte), die übrigen ca. 25 der 32 Lexikon-Arten haben keine eigene Zeitreihe, nur einen statischen Steckbrief. War bereits vom Nutzer selbst in `Ideen_Backlog_Waldsimulation_Klasse8.md` unter „Mehr Tiere und Pflanzen bei der Simulation anzeigen" vorgemerkt; dort um den Kurven-Analyse-Aspekt ergänzt. Auf Nutzer-Entscheidung nicht in dieser Session umgesetzt (größere fachliche Modellierungsentscheidung, kein kleiner Fix).
- **Recherche-Vorarbeit (2026-08-26), fachliche Grundlage für „weitere Arten" vorbereitet:** Auf Nutzerwunsch per Subagent recherchiert und in neuer Datei `docs/Wissensbasis_Erweiterung_weitere_Arten.md` festgehalten: für 19 der bislang unsimulierten Arten (Sträucher, Pilze, Kräuter, Eichhörnchen/Raupen/Blattläuse, Eichelhäher, alle 7 Prädatoren) jeweils Störungsreaktion, Kopplungsvorschlag an bestehende Indikatoren und Einschätzung der Umsetzungssicherheit (gut belegt vs. „Annahme, mit Lehrkraft abzugleichen" vs. strukturelle Lücke wie beim Fuchs, der an einen aktuell nicht simulierten Kleinsäuger-Indikator gekoppelt wäre). Ergänzt, editiert aber nicht das Original-Wissensbasis-Dokument (bleibt unverändertes Lehrkraft-Liefermaterial). Reine Recherche, keine Freigabe zur Umsetzung - siehe Backlog-Ergänzung.
- **Erweiterung (2026-08-26), 20 neue Arten-Indikatoren im Analyse-Screen (Umsetzung von Backlog-Punkt „Mehr Tiere und Pflanzen"):** Auf Nutzerentscheidung (nur Analyse-Screen, kein Dashboard/Netzwerk-Graph/Forscherheft in dieser Runde) wurde die o.g. Recherche in `scripts/simulation/model.py` umgesetzt - plus vorgeschaltetem gezieltem Nachrecherche-Schritt, der mehrere Formeln mit echten Quellen unterlegt hat (siehe „Nachrecherche zur Unsicherheits-Reduktion"-Abschnitt in `docs/Wissensbasis_Erweiterung_weitere_Arten.md`, u. a. Kleinsäuger jetzt an Mastjahr-Nahrungsangebot statt frei erfundener Formel gekoppelt, Blattläuse enger an geschwächte Fichte). Neu simuliert: Hasel/Holunder/Brombeere (Sträucher), Zunderschwamm/Bläuepilz/Hallimasch (Pilze), Brennnessel/Buschwindröschen/Waldmeister/Heidelbeere (Kräuter), Eichelhäher (Samenverbreiter), Eichhörnchen/Raupen/Blattläuse (weitere Pflanzenfresser), Buntspecht/Ameisenbuntkäfer/Habicht/Sperber/Fuchs (Prädatoren) sowie ein neuer, komplett erfundener `kleinsaeuger_indikator` als Modellierungs-Voraussetzung für den Fuchs (kein eigener Lexikon-Knoten, strukturelle Ergänzung, klar im Code/`data/indikatoren.json` gekennzeichnet). `data/indikatoren.json` von 14 auf 34 Einträge erweitert; `js/analyse.js` um Farben (`INDIKATOR_FARBEN`), neue Kategorie-Labels (`KATEGORIE_LABEL`: Sträucher/Pilze/Krautschicht/Samenverbreiter/Pflanzenfresser/Prädatoren/Sonstige Tiergruppen) und kuratierte Kaskaden-Vorauswahl-Ergänzungen (`KASKADE_JE_STOERUNG`/`KASKADE_WILDVERBISS`) ergänzt. `js/dashboard.js`/`js/forscherheft.js`/`js/graph.js`/`data/edges.json` bewusst unangetastet (Nutzer-Vorgabe) - drei Kräuter (Waldmeister, Heidelbeere, Brennnessel) haben deshalb weiterhin keine Kante im Netzwerk-Graph, ihre Kopplung existiert nur in `model.py`.
  **Beim Implementieren zwei weitere Bugs desselben Musters gefunden und behoben** (Bestandsanteil/Abwesenheit mit Vitalität/Gesundheit verwechselt, wie beim Fichte-Fix oben): (1) Buschwindröschen fiel auch OHNE jede Störung von 40 auf ~7 über 20 Jahre, weil seine Zielformel bei ungestörtem Kronendach (=100) immer auf 0 konvergierte statt einen stabilen Basiswert zu halten - behoben durch eine Zielformel mit echtem, störungsunabhängigem Sockelwert (40), der durch Kronendachverlust zusätzlich ansteigt. (2) Blattläuse stiegen im Kiefernwald (wo es gar keine Fichte gibt) von 0 auf 30, weil die Formel `100 - fichte_vitalitaet` "keine Fichte vorhanden" (Wert dauerhaft 0) fälschlich als "Fichte komplett geschwächt" interpretierte - behoben durch Bezug auf den tatsächlichen Ausgangswert (`self._anfangsfichte`, bereits bestehendes Muster aus der Borkenkäfer-Wirt-Verfügbarkeits-Berechnung) statt auf die feste Zahl 100. Beide Bugs wurden durch einen systematischen Baseline-Drift-Test (alle 3 Waldtypen ohne jede Störung über 20 Jahre, keiner der neuen Indikatoren darf ohne Ursache driften) beim Verifizieren gefunden, nicht durch Zufall.
  **Verifiziert:** alle 195 Zeitreihen neu gebaut (Kombinatorik unverändert, nur neue Felder), programmatischer Wertebereichs-Check über alle 198 simulierten Kombinationen (0-100, keine Verletzung), Baseline-Drift-Test (s. o., nach den zwei Fixes sauber), Kontrastproben (Zunderschwamm/Hallimasch/Buntspecht brechen bei Totholzentnahme mit ein, Bläuepilz folgt Borkenkäfer 1 Jahr verzögert). Zusätzlich per jsdom (echter statischer HTTP-Server, `WaldsimAnalyse.start()` direkt mit einem Borkenkäfer-Lauf aufgerufen) Ende-zu-Ende geprüft: genau 34 Checkboxen, alle 14 Kategorie-Gruppen mit sprechendem deutschem Label (keine rohe Kategorie-ID im Markup), keine graue Fallback-Farbe, Kaskaden-Vorauswahl zieht die neuen kaskadenrelevanten Indikatoren korrekt mit rein (Borkenkäfer → zusätzlich Bläuepilz/Buntspecht/Ameisenbuntkäfer vorausgewählt), Kurven-Rendering (Anzahl Polylinien wechselt korrekt beim Toggle einer zusätzlichen Checkbox, keine NaN/undefined in den Kurvenpunkten). Bestehende Akzeptanzkriterien erneut geprüft, unverändert erfüllt (Borkenkäfer+Fichtenmonokultur=Kollaps, Borkenkäfer+Mischwald=lokal - Fichte-Endwert im Mischwald von 85.7 auf 90.6 leicht verändert, weil die neue Buntspecht/Ameisenbuntkäfer-Dämpfung jetzt auch real in die Borkenkäfer-Schadensrechnung eingreift, bleibt aber klar im „lokal"-Bereich). `sw-precache-manifest.json`/`sw.js` neu gebaut.
- **Erweiterung (2026-08-26), Wildverbiss-Regler durch Wolf/Luchs-Schalter ersetzt (Nutzerentscheidung, Rücksprache im Chat):** Der abstrakte 3-Stufen-Regler (niedrig/mittel/hoch) für die Wilddichte ist ersetzt durch zwei unabhängige, standardmäßig aktive Schalter „Luchs" und „Wolf" im Startbildschirm - Ausgangszustand (Play gesperrt) ist jetzt „beide anwesend" (entspricht inhaltlich dem früheren „niedrig"), das Entfernen eines oder beider Prädatoren ist die vom Nutzer gewählte Abweichung. Macht die Ursache (fehlende Prädatoren, siehe Wissensbasis-TL;DR „150 Jahre ausgerottet, erst seit rund zwei Jahrzehnten regional zurückgekehrt") statt nur ihrer abstrahierten Wirkung bedienbar - passt besser zum Rachel-Carson-Kernthema des Projekts (M8). Gleichzeitig werden Reh und Rothirsch erstmals als getrennte Bestandsgrößen simuliert (`reh_dichte`/`rothirsch_dichte` statt einem gemeinsamen `wilddichte`-Indikator), weil Luchs und Wolf laut Nachrecherche (siehe „Nachrecherche"-Abschnitt in `docs/Wissensbasis_Erweiterung_weitere_Arten.md`, Punkt 6) unterschiedlich stark auf beide Arten wirken: Luchs ist laut KORA-Radiotelemetrie ein Reh-Spezialist (Rothirsch taucht dort praktisch nicht als Beute auf), Wolf nimmt laut DBBW/Senckenberg-Kotprobenanalyse deutlich mehr Rothirsch, aber klar seltener als Reh (Verhältnis grob 2:1 bis 4:1). Diätanteile sind kein direktes Maß für Regulationswirkung pro Kopf - die Übertragung in die Reduktionsfaktoren `LUCHS_REDUKTION`/`WOLF_REDUKTION` in `scripts/simulation/model.py` bleibt eine Modellierungs-Annahme, jetzt aber mit echter Zahlengrundlage statt freier Schätzung (mit Lehrkraft abzugleichen).
  **Betroffene Dateien:** `scripts/simulation/model.py` (Konstruktor-Signatur `wolf_aktiv`/`luchs_aktiv` statt `wildverbiss_stufe`, neue Zielwert-Formel für `reh_dichte`/`rothirsch_dichte`, `basis_rate` für Verjüngungs-/Eiche-Effekt jetzt linear aus der kontinuierlichen Dichte statt aus einer Stufen-Tabelle abgeleitet), `data/stoerungen.json` (`wildverbiss_regler.stufen` → `praedatoren`-Array + `code_werte`), `scripts/simulation/build_simulationen.py` (neue Kombinatorik-Achse: 4 Prädatoren-Codes `beide`/`luchs`/`wolf`/`keine` statt 3 Regler-Stufen, Dateiname-Segment `praedatoren-<code>` statt `regler-<stufe>`), `js/simulationConfig.js` (`dateiname()`/`istGueltigeAbweichung()` entsprechend angepasst), `js/startScreen.js` (zwei Checkbox-Schalter statt 3-Segment-Regler, wiederverwendet dieselben `.regler-segmente`/`.regler-segment`-CSS-Klassen ohne CSS-Änderung), `data/indikatoren.json` (`wilddichte` → `reh_dichte` + `rothirsch_dichte`), `js/analyse.js` (Farben/Kaskaden-Liste entsprechend nachgezogen).
  **Notwendige Mini-Fixes in `js/graph.js` (2 Stellen) und `js/forscherheft.js` (1 Stelle):** beide enthielten einen String-Vergleich `regler !== "niedrig"` (Kaskaden-Relevanz-Filter bzw. „Mischwald verarmt zum Buchen-Reinbestand"-Bilderkennung), der mit den neuen Regler-Werten nie mehr träfe - auf `!== "beide"` aktualisiert. Reine Konsequenz der Werteraum-Umbenennung, keine Funktions-/Design-Änderung an diesen Dateien, die sonst wie in der vorigen Erweiterung unangetastet bleiben.
  **Kombinatorik-Änderung:** 22 Ereigniszustände × 4 Prädatoren-Codes − 1 ungültige „nichts gewählt" = 87 Konfigurationen pro Waldtyp × 3 Waldtypen = 261 Zeitreihen (bisher 195). Weicht vom in `docs/Umsetzungsauftrag_Waldsimulation_Klasse8_fuer_Claude_Code.md` dokumentierten Akzeptanzkriterium „195 Kombinationen" ab; dort noch nicht nachgezogen (Rang-1-Quelldokument, siehe `data/stoerungen.json` → `kombinatorik.kombinatorik_aenderung_hinweis`).
  **Verifiziert:** alle 261 Zeitreihen neu gebaut, programmatischer Wertebereichs-Check (0-100, keine Verletzung). Eckwerte der neuen Formel stichprobenartig nachgerechnet (Mischwald, Jahr 20): kein Prädator → Reh/Rothirsch 80/80, nur Luchs → 16/72, nur Wolf → 36/56, beide → 7/50 - exakt wie geplant. „Wildverbiss erst 2. Hälfte sichtbar" erneut geprüft und weiterhin erfüllt (Steigungswechsel bei `verjuengung_mischbaumarten` weiterhin bei Jahr 10, absolute Endwerte im „kein Prädator"-Fall mit dem früheren „hoch" vergleichbar). Zusätzlich per jsdom den kompletten Nutzerfluss Ende-zu-Ende geprüft (echter statischer HTTP-Server, echte DOM-Interaktion statt direkter Funktionsaufrufe): Luchs/Wolf-Schalter starten beide angehakt, Play bleibt gesperrt ohne Abweichung („Wähle mindestens eine Störung oder entferne Luchs und/oder Wolf."), Play wird frei sowohl beim Abwählen eines Prädators als auch bei einer Ereignis-Störung, Dashboard → Analyse (35 Checkboxen, `reh_dichte`/`rothirsch_dichte` vorhanden, `wilddichte` verschwunden) → Reflexion → Forscherheft-Speicherung laufen fehlerfrei durch, gespeicherter Eintrag zeigt korrekt „Wildverbiss-Regler: Beide". `js/graph.js`-Kaskaden-Filter nur per Code-Review geprüft (reiner String-Vergleich, kein UI-Testaufbau für die Rachel-Carson-Freischaltung in dieser Runde). `sw-precache-manifest.json`/`sw.js` neu gebaut.
- **Erweiterung (2026-08-26), Test-Werkzeuge: Zurücksetzen-Button + Versionsanzeige:** Auf Nutzerwunsch zwei kleine technische Ergänzungen, damit sich die App leichter testen lässt: (1) Ein neuer Button „🔄 Zurücksetzen" im Startbildschirm-Header (neben Forscherheft/Netzwerk/Lexikon-Buttons, `js/app.js`) löscht nach Bestätigungsdialog alle `waldsim.*`-localStorage-Einträge (neue Funktion `WaldsimStorage.clearAll()` in `js/storage.js`, iteriert über alle Keys mit dem Präfix statt einzelne Keys hart zu kodieren) und lädt die Seite neu - anders als der bereits bestehende „Neue Sitzung starten"-Button im Forscherheft (löscht nur die gespeicherten Einträge, gedacht für Geräte-Weitergabe an eine andere Klasse) setzt dieser auch die Netzwerk-Graph-Freischaltung („Wissenschaftlerin/Wissenschaftler um Hilfe bitten") zurück, um den kompletten Erststart-Zustand für einen neuen Testlauf wiederherzustellen. (2) Eine kleine Versionskennung (`#app-version-hinweis`, unterhalb des Status-Hinweises auf jedem Screen sichtbar) zeigt die ersten 8 Zeichen des ohnehin bei jedem Build von `scripts/pwa/build_precache_manifest.py` neu berechneten Inhalts-Hashs aus `sw-precache-manifest.json` (per `fetch` zur Laufzeit geladen) - ändert sich automatisch bei jeder Content-Änderung, kein zusätzlicher Build-Schritt nötig, dient als Referenz beim Rückmelden von Testergebnissen und zur Prüfung, ob wirklich die neueste Version getestet wird.
  **Verifiziert:** Per jsdom (echter statischer HTTP-Server) geprüft: Versionsanzeige zeigt den erwarteten 8-stelligen Hash-Ausschnitt, Zurücksetzen-Button vorhanden und löscht sowohl einen simulierten Forscherheft-Eintrag als auch die Netzwerk-Freischaltung aus `localStorage` beim Klick (Bestätigungsdialog gemockt); `location.reload()` wird danach angestoßen (von jsdom selbst nicht ausführbar, „Not implemented: navigation to another Document" ist die dafür erwartete jsdom-Fehlermeldung, kein App-Fehler). `sw-precache-manifest.json`/`sw.js` neu gebaut.
- **M11-Test, Akzeptanzkriterien-Abgleich (2026-08-26):** Alle 13 Kriterien aus Umsetzungsauftrag Abschnitt 6 (Rang-1-Quelle, ersetzt die ältere Liste in Technikdokument Abschnitt 8) einzeln geprüft – per neu geschriebenem jsdom-Ende-zu-Ende-Test gegen einen echten lokalen HTTP-Server (nicht nur Code-Review), plus direkter Auswertung der generierten Zeitreihen-JSONs für die quantitativen Kriterien:
  1. „3 Waldtypen + 5 Ereignis-Störungen wählbar, 0–2 kombinierbar, Naturereignis/Bewirtschaftung visuell unterscheidbar" – ✅ per E2E-Test bestätigt (`kategorie-naturereignis`/`kategorie-bewirtschaftung`-Klassen im Markup vorhanden).
  2. „Zwei Wald-Plätze zwingend, immer unterschiedliche Typen" – ✅ per E2E-Test bestätigt (Radio für bereits gewählten Typ bei Wald 2 wird `disabled`).
  3. „Borkenkäfer + Fichtenmonokultur = großflächiger Kollaps, Mischwald = nur lokaler Schaden" – ✅ per Datenauswertung bestätigt: Fichtenmonokultur `fichte_vitalitaet` 100→0, `gesamtvitalitaet` 100→13,9; Mischwald `fichte_vitalitaet` 100→90,6, `gesamtvitalitaet` 100→99,5.
  4. „Höhere Temperaturen als durchgehender Hintergrundfaktor ab Jahr 0, bei Kombination entfällt Reihenfolge-Auswahl" – ✅ per `data/stoerungen.json` (`temperatur_kombinationen`) bestätigt, unverändert seit M4.
  5. „Wildverbiss-Regler (3 Stufen), unabhängig von Ereignisauswahl, als „Strukturelles Ungleichgewicht" gekennzeichnet" – ⚠️ **teilweise überholt, aber Absicht erfüllt:** Der 3-Stufen-Regler wurde am 2026-08-26 bewusst durch die Luchs/Wolf-Schalter ersetzt (siehe Eintrag oben); Kategorie-Label „Strukturelles Ungleichgewicht" steht weiterhin über dem Regler-Abschnitt, Unabhängigkeit von der Ereignisauswahl per E2E-Test bestätigt. Die Rang-1-Quelle (Umsetzungsauftrag) nennt noch „3 Stufen" – das ist eine dokumentierte, bewusste Abweichung (siehe dortiger Log-Eintrag), kein neuer Fund.
  6. „Regler=hoch + 0 Ereignisse zeigt im Mischwald erst in 2. Hälfte spürbare Verjüngungs-Verarmung" – ✅ per Datenauswertung bestätigt (Analogfall „keine Prädatoren": `verjuengung_mischbaumarten` fällt Jahr 0–10 nur 100→96,8, Jahr 10–20 dagegen 96,8→75,5).
  7. „Play gesperrt, solange weder Ereignis noch Regler vom Standard abweicht" – ✅ per E2E-Test bestätigt (inkl. Wechsel gesperrt→frei→gesperrt→frei bei Prädator-Abwahl bzw. Ereignis-Auswahl).
  8. „Alle gültigen Kombinationen vorab berechnet, keine Laufzeit-Neuberechnung" – ✅ bestätigt, aber Anzahl weicht von der im Kriterium genannten „195" ab: tatsächlich 261 (Folge der Luchs/Wolf-Erweiterung, bereits im „Erweiterung"-Log-Eintrag oben als Abweichung von der Rang-1-Quelle dokumentiert). Laufzeitseitig bestätigt: `js/startScreen.js` lädt ausschließlich vorab generierte Dateien aus `data/generated/simulationen/`, keine Live-Berechnung im Browser.
  9. „Netzwerk-Graph zu Beginn gesperrt, entsperrt erst nach Rachel Carson" – ✅ per E2E-Test bestätigt, inkl. toleranter Namensprüfung (" rachel CARSON " mit Leerzeichen/Groß-Kleinschreibung).
  10. „Analyse-Screen mit frei kombinierbaren Kurven vom Live-Dashboard getrennt" – ✅ per E2E-Test bestätigt (eigener Screen-Wechsel, 35 Indikator-Checkboxen).
  11. „Arten-Lexikon (32 Knoten, einfache Liste, keine Suche) unabhängig vom Graphen erreichbar" – ✅ per E2E-Test bestätigt (genau 32 `.lexikon-karte`-Elemente, aufrufbar ohne jede Freischaltung).
  12. „App funktioniert nach einmaligem Laden vollständig offline" – ✅ strukturell bestätigt: `sw.js` cached alle Dateien aus `sw-precache-manifest.json` (341 Dateien, Version `d27297df7a232e14`), `SHELL_FILES`/verzeichnisbasierte Erfassung in `scripts/pwa/build_precache_manifest.py` deckt `index.html`, `css/`, `js/`, `assets/`, `data/`, `manifest.json` vollständig ab (kein manuell gepflegter, potenziell unvollständiger Dateiname mehr, siehe „Kritischer Fix nach Nutzer-Feedback (2026-08-26), Service-Worker-Update-Erkennung" oben). Kein isolierter Flugmodus-Test in dieser Session möglich (keine Browser-Umgebung), aber Mechanismus seit dem Service-Worker-Update-Fix strukturell verifiziert.
  13. „Bedienung für Achtklässler:innen ohne Erklärung selbsterklärend" – 🔍 **ein konkreter Fund und behoben:** Der Hinweistext unter „3 · Prädator-Beute-Regulation" in `index.html` lautete noch „Standard: niedrig (kaum Effekt)" – ein Wortlaut aus dem alten 3-Stufen-Regler, der seit der Luchs/Wolf-Umstellung nirgends mehr in der UI vorkommt und potenziell verwirrt („niedrig" wonach?, es gibt keine Stufen-Auswahl mehr zu sehen). Behoben zu „Standard: Luchs und Wolf beide anwesend (kaum Effekt)". `sw-precache-manifest.json`/`sw.js` danach neu gebaut. Ansonsten subjektiv/pädagogisch, nicht automatisiert prüfbar – keine weiteren Stellen mit vergleichbar veraltetem Wortlaut gefunden (gezielt nach „niedrig/mittel/hoch" und „Wildverbiss-Regler" in `index.html`/`js/*.js`/`css/*.css` gesucht).

  **Testaufbau:** Neu geschriebenes jsdom-Skript (nicht Teil des Repos, nur für diese Session, da kein `package.json`/Test-Tooling vorhanden ist – wie schon in früheren Milestones üblich) gegen einen echten `python3 -m http.server` auf Port 8791, mit `window.fetch`-Polyfill auf Basis von Node's nativem `fetch` (jsdom bringt selbst kein `fetch` mit). Deckt den kompletten Nutzerfluss ab: Netzwerk-Sperre → Lexikon → Wald-Auswahl (inkl. Deaktivierungslogik) → Prädatoren-Schalter → Ereignis-Auswahl → Play-Freischaltung → Dashboard → Jahr-20-Sprung → Analyse (35 Checkboxen) → Snapshot → Reflexion → Forscherheft-Speicherung → erneute Netzwerk-Freischaltung aus dem Forscherheft heraus → Brief-Screen. Alle 29 Prüfpunkte grün. **Eigener Debugging-Fund während der Testerstellung (kein App-Bug):** Ein erster Testlauf zeigte scheinbar kaskadenartige Fehlschläge ab der Wald-2-Auswahl – Ursache war ein Fehler im Testskript selbst (gecachte `NodeList`-Referenzen auf Radio-Buttons, die nach dem `innerHTML`-Neu-Rendern von `#wald-slots` bei jeder Wald-Auswahl – siehe `renderWaldSlots()` in `js/startScreen.js` – vom DOM abgehängt waren und deshalb keine Events mehr bubbelten). Nach Korrektur (Radios nach jeder Interaktion frisch abfragen statt zu cachen) liefen alle Folgeschritte fehlerfrei durch – bestätigt, dass die App selbst korrekt funktioniert.

- **M11-Test, Deployment (2026-08-26):** `https://dragongame.github.io/Wald-simulation/` liefert HTTP 200 (per `curl` bestätigt) – GitHub Pages ist bereits aktiv und aktuell (Stand des zuletzt gepushten Commits `e0456c0`); die heute in dieser Session vorgenommenen Änderungen (Backlog→Milestones-Umbau, Regler-Hinweistext-Fix) sind lokal, aber noch nicht gepusht. `manifest.json` (`start_url`/`scope`: `"./"`) und Service-Worker-Registrierung (`navigator.serviceWorker.register("./sw.js")` in `js/app.js`) verwenden weiterhin ausschließlich relative Pfade – Voraussetzung für den GitHub-Pages-Unterordner-Betrieb, unverändert seit M3.
- **M11-Ergänzung, QR-Code (2026-08-26):** `docs/QR-Code_Wald-simulation.png` neu erzeugt (öffentlicher, keine Anmeldung erfordernder QR-Generator-Dienst, 600×600px, kodiert `https://dragongame.github.io/Wald-simulation/`) – für Beamer/Whiteboard-Anzeige im Unterricht gemäß Technikdokument 5.7. Bewusst unter `docs/` (Projektmaterial für die Lehrkraft), nicht unter `assets/` – kein Teil der App selbst, daher auch nicht im Service-Worker-Precache (`scripts/pwa/build_precache_manifest.py` erfasst nur `assets/`/`data/`/`css/`/`js/`/Kern-Dateien, nicht `docs/`).
- **M13 (2026-08-26), Reihenfolge-Abfrage nur bei echtem Zeitversatz:** `data/stoerungen.json` → `paar_zeitabstaende` → Eintrag `trockenheit`+`borkenkaefer` (`abstand_jahre: 0`) von `reihenfolge_waehlbar: true` auf `false` umgestellt (neues Feld `reihenfolge_waehlbar_hinweis` dokumentiert die Begründung direkt im Datensatz). Reine Datenänderung – die Auflösungslogik in `js/simulationConfig.js` (`resolveEreignisKonfiguration`) unterstützt den Fall „Reihenfolge nicht wählbar" bereits seit M4 (kanonische Reihenfolge nach `EVENT_ORDER`, ursprünglich für die drei Totholzentnahme-Paare gebaut), keine Code-Änderung nötig. **Verifiziert vor der Umstellung:** `data/generated/simulationen/<waldtyp>__borkenkaefer-dann-trockenheit__praedatoren-beide.json` und `..._trockenheit-dann-borkenkaefer_...json` sind für alle 3 Waldtypen bis auf die reine Reihenfolge-Angabe im `konfiguration`-Feld byte-identisch (0 Unterschiede in den 21 Zeitreihen-Jahreswerten je Waldtyp) – bei `abstand_jahre=0` triggern beide Ereignisse gleichzeitig in Jahr 0, das Modell kennt also gar keinen Unterschied zwischen den Reihenfolgen. Bei den beiden verbleibenden reihenfolge-wählbaren Paaren (Sturm+Borkenkäfer, Sturm+Trockenheit, beide `abstand_jahre: 1`) bleibt die Abfrage bestehen, weil dort die Reihenfolge real unterschiedliche Zeitreihen erzeugt (das zuerst eintretende Ereignis trifft den ungeschwächten Bestand, das zweite bereits den vorgeschädigten). Per jsdom-E2E-Test (echter HTTP-Server) bestätigt: Trockenheit+Borkenkäfer zeigt keine Reihenfolge-Abfrage mehr und startet direkt; Sturm+Borkenkäfer verlangt weiterhin eine Wahl (Play bleibt bis dahin gesperrt) und schaltet nach Auswahl frei. `sw-precache-manifest.json`/`sw.js` neu gebaut (Version `a9a97f0818eaaf86`).
- **Priorisierungsentscheidung (2026-08-26), Reihenfolge M12–M22 festgelegt:** Auf Nutzerwunsch („Leg die Reihenfolge fest") eigenständig priorisiert (siehe Liste oben in der Tabellen-Sektion), nach folgenden Kriterien: (1) triviale, unabhängige Fixes zuerst (M13), (2) direkte Fortsetzung zuletzt bearbeiteten Codes, solange der Kontext noch frisch ist (M12), (3) inhaltliche Abschlüsse bestehender Milestones vor neuen Baustellen (M16 schließt M6/M7 ab), (4) risikoarme, komplett unabhängige Punkte dazwischen (M19), (5) Struktur vor Optik – M14 (Auswahl vereinfachen) vor M20 (Feldbuch-Optik), damit dieselbe UI nicht zweimal umgebaut wird, (6) explizite inhaltliche Abhängigkeit beachtet – M18 (Drag & Drop „ins Buch") ergibt laut Backlog-Formulierung erst nach M20 Sinn, (7) rein dekorative/optionale Punkte (M17) sowie die schon ursprünglich für „nicht Version 1" vertagten, aufwändigeren Punkte (M21 PDF-Export, M22 Vorführ-Modus) ans Ende. Reine Priorisierung, keine Umsetzung – die Reihenfolge kann bei neuen Erkenntnissen (z. B. wenn sich beim Umsetzen von M14 zeigt, dass M20 doch vorgezogen werden sollte) jederzeit angepasst werden.
- **Planungsentscheidung (2026-08-26), Ideen-Backlog zu Milestones M12–M22 gemacht:** `docs/Ideen_Backlog_Waldsimulation_Klasse8.md` wurde durchgegangen und bis auf einen Punkt vollständig in benannte, noch offene Milestones überführt (siehe Tabelle oben); das Backlog-Dokument selbst wurde danach bis auf die einleitende Erklärung geleert. Zwei Nutzerentscheidungen dabei: (1) „Feldbuch-Feeling" bleibt bewusst **ein** großer Milestone (M20) statt in mehrere kleinere aufgeteilt zu werden – Teilschritte werden wie bisher üblich beim Umsetzen hier im Lücken-Log dokumentiert, nicht vorab in der Tabelle granularisiert. (2) M11 (Test & Deployment) ist explizit als **nächster** Schritt festgelegt, vor allen neuen M12+-Milestones – V1 soll technisch abgeschlossen/deploybar sein, bevor weitere Features dazukommen. Der Backlog-Punkt „Brände simulieren" wurde bewusst **nicht** zu einem Milestone gemacht und bleibt im (sonst geleerten) Ideen-Dokument als offene Diskussionsfrage stehen, da er laut eigenem Backlog-Eintrag noch fachlich/didaktisch abgewogen werden muss (neue Störungsart, keine kleine Modellerweiterung) – vor Umsetzung erst eine eigene Diskussion nötig, kein reiner Umsetzungs-Milestone wie die übrigen.
- **Kleine Ergänzung (2026-08-26), Luchs/Wolf-Schalter mit Portrait-Bildern statt reinem Text:** Auf Nutzerwunsch nachgeliefert - die beiden Prädatoren-Checkboxen im Startbildschirm zeigen jetzt `luchs_portrait.png`/`wolf_portrait.png` (bereits seit dem Arten-Lexikon, M9, vorhanden - keine neuen Grafiken nötig, referentielle Prüfung aller 35 Knoten × Sprite-Zustandsvarianten bestätigte lückenlose Bildabdeckung). `js/startScreen.js`: neue Konstante `PRAEDATOR_EMOJI` (Fallback-Emoji), `renderRegler()` iteriert jetzt datengetrieben über `data.stoerungen.wildverbiss_regler.praedatoren` statt zwei Labels hart zu kodieren. Keine CSS-Änderung nötig (wiederverwendet unverändert `.regler-segment`, Bild+Text stapeln sich durch die bestehende Block-Struktur). Per jsdom geprüft: beide Bilder korrekt referenziert (`./assets/sprites/luchs_portrait.png`/`wolf_portrait.png`), kompletter Nutzerfluss weiterhin fehlerfrei.
- **M12 (2026-08-26), Dashboard-Kacheln für die 20 neuen Arten-Indikatoren:** Die 19 in der „Erweiterung ... 20 neue Arten-Indikatoren"-Runde ergänzten Lexikon-Arten (Sträucher, Pilze, Krautschicht, Samenverbreiter, weitere Pflanzenfresser, Prädatoren) haben jetzt eigene Live-Dashboard-Kacheln, nicht mehr nur eine Kurve im Analyse-Screen. `kleinsaeuger_indikator` bleibt bewusst ausgenommen (rein strukturelle Hilfsgröße ohne eigenen Lexikon-Knoten/Sprite, siehe `data/indikatoren.json`). Jede dieser Arten hat laut `data/nodes.json` nur eine einzige Sprite-Zustandsvariante (`<art>_portrait.png`, kein Crossfade über Schwellenwerte wie bei der Fichte) - referentiell geprüft, dass alle 19 Dateien unter `assets/sprites/` existieren und alle 19 Indikator-IDs in `data/indikatoren.json` sowie alle 19 Node-IDs in `data/nodes.json` vorkommen.
  **Umsetzungsentscheidung, einklappbarer Bereich statt fixer Erweiterung der Haupt-Kachelreihe:** Die 6 ursprünglichen Baumarten/Borkenkäfer/Totholz-Kacheln (M5) bleiben unverändert die primäre `.art-reihe`. Da bei den meisten Szenarien (Modell-Baselinewerte, siehe `scripts/simulation/model.py`) 13-17 der 19 neuen Arten schon ohne jede Störung „relevant" (Wert > 0 irgendwann im 20-Jahres-Verlauf) sind, hätte ein einfaches Anhängen an die Hauptreihe die für Achtklässler:innen zentralen 6 Feldmessinstrumente/Baumarten optisch erschlagen (Umsetzungsauftrag-Leitprinzip „selbsterklärend ohne Erklärung"). Stattdessen ein neuer, nach Kategorie gruppierter, standardmäßig eingeklappter Bereich „Weitere Arten anzeigen (n)" (`<details>`/`<summary>`, kein externes UI-Framework nötig) am Ende jedes Wald-Panels, mit kleineren Kacheln (`.art-sprite--klein`, 3rem statt 4.2rem) als die Haupt-Baumarten. Gruppierung/Label/Emoji-Fallback folgen derselben Kategorie-Konvention wie Lexikon (M9) und Netzwerk-Graph (M8) - dort jeweils eine eigene, screen-lokale `KATEGORIE_LABEL`/`KATEGORIE_EMOJI`-Konstante statt eines geteilten Moduls, hier fortgeführt (`WEITERE_ARTEN_KATEGORIE_LABEL`/`_EMOJI` in `js/dashboard.js`).
  **Code-Struktur:** `ermittleRelevanteArten()` generalisiert (nimmt jetzt eine Arten-Liste als Parameter statt hart `ARTEN`), `updateArtenFuerWald()` in eine gemeinsame `updateArtenListe(i, zr, artenListe, idPraefix)` extrahiert (Präfix `art` für die Haupt-Baumarten, `weitere` für die neuen Kacheln) - reine Wiederverwendung der bestehenden Crossfade-/Opacity-Logik aus M5, keine neue Update-Mechanik nötig, da jede neue Art dieselbe `zustaende`-Struktur wie Eiche/Birke (eine einzige Zustandsvariante) verwendet.
  **Verifiziert:** Per jsdom Ende-zu-Ende geprüft (echter statischer HTTP-Server, zwei unabhängige Testläufe): (1) Fichtenmonokultur+Borkenkäfer/Mischwald-Vergleich - genau 19 Kacheln pro Panel möglich, Fichtenmonokultur zeigt nur 17 (Eichelhäher und Fuchs bleiben 0, da diese Art an Eiche/Buche-Vitalität gekoppelt ist und die Fichtenmonokultur laut `data/waldtypen.json` keine/kaum Eiche/Buche führt - ökologisch korrektes Verhalten, keine fehlerhafte Filterung), Bläuepilz-Kachel zeigt bei Jahr 0 erwartungsgemäß Opacity 0.18 (Borkenkäferdichte noch 0) und wird bei Jahr 10 sichtbar (Opacity 1, folgt der Borkenkäferdichte mit Verzögerung, wie in `model.py` vorgesehen). (2) Mischwald+Kiefernwald ohne Ereignis, nur Luchs abgewählt (Play-Freischaltung über Prädator-Abweichung) - Dashboard startet fehlerfrei, Play/Pause/Einzelschritt funktionieren unverändert neben den neuen Kacheln (Jahr-Fortschritt korrekt), unterschiedliche Kachelanzahl je Waldtyp (Mischwald 13, Kiefernwald 8 - plausibel, da z. B. Waldmeister/Heidelbeere je nach Waldtyp unterschiedlich gewichtet sind). `sw-precache-manifest.json`/`sw.js` neu gebaut (Version `3df0a66e78bc8136`).
